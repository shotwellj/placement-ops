# Session log — 2026-04-24 — Phase A4 finalization, Phase B1, Phase B2

## What shipped today

| Phase | Status | Real impact |
|-------|--------|-------------|
| Phase A4 | Shipped | AIR Blackbox compliance scan: 18 PASS → 21 PASS, 7 FAIL → 4 FAIL |
| Phase B1 math | Shipped | Bayesian-dampened adjacency calibration, deterministic, audit-traced |
| Phase B1 endpoint+UI | Shipped | Stage buttons live on every submission row in `/app/pipeline.html` |
| Company override rule | Shipped | JD body overrides user-typed company. Bug class fixed (SeeScan/Qualcomm) |
| Phase B2 full slice | Shipped | 18 alias/promote/reject decisions, **6%→33% req resolution, 40%→58% candidate resolution** |

## Schema changes

- **Migration 004** (calibration) — extended `calibration_events` with `req_id`, `from_stage`, `to_stage`, `event_weight`, `audit_event_id`. New tables: `calibration_runs`, `adjacency_history`.
- **Migration 005** (skill resolution) — new tables: `skill_resolution_decisions` (alias/promote/reject), `skill_promotion_suggestions` (LLM cache).

## Real numbers at session end

- Reqs: 7 (5 Qualcomm + 1 SeeScan + 1 Northrop Grumman)
- Submissions: 1 (Steve Benz on Northrop — first-ever real submission, transitioned via stage buttons)
- req_skills: 148 total, 49 resolved (33.1%), 99 unresolved
- candidate_skills: 33 total, 19 resolved (57.6%)
- Skills in taxonomy: ~200 (197 from seed + ~5 added implicitly via aliases extending existing skill rows)
- skill_resolution_decisions: 18 (13 alias + 5 reject + 0 promote)
- audit_events: includes 7 alias_skill + 1 reject_skill + recent ai_decision events from intakes
- Production database is at Turso `sourcingnav-prod`, primary region aws-us-west-2

## Critical bug fixes shipped

1. **`params is not defined` on pipeline.html** — pre-existing latent bug from commit `faea53e` (April 21), masked by no error handler. Fixed in commits `9b9beb0` (global error handler added) + `a2b086b` (params declaration restored). Lesson: always load existing pages before deploying new code on top.

2. **Diagnostic logging in `get_req`** — added in commit `1cd2209` to chase a 404, removed in `4446bd1` after the 404 turned out to be browser cache.

3. **`_calibration.py` interface mismatch** — initially used raw row lists, production client returns `_Result.rows`. Caught and fixed in 7 sites before any production call.

4. **AI parse ReadTimeout** — added 503 with friendly message + diagnostic logging in `e4f5527`. Not a permanent fix; eventual answer is streaming responses or BYOK provider switch.

## Architectural decisions locked in

- **JD body is source of truth for company.** User-typed company is a fallback only. Override is silent + visible amber banner + audit_event. (`api/index.py` intake)
- **Real-time calibration on every stage change.** Runs immediately, processes ALL pending events. No nightly batch needed.
- **Manual approval for taxonomy changes (B2).** Pure manual approval. LLM suggests, user decides. Auto-promotion deferred to future B2.5.
- **Stage signal weights:** placed=+3, offer=+2, onsite=+1, phone_screen=+0.5; reject from submitted=-1, phone_screen=-1.5, onsite=-2, offer=-2.5

## What's still open

1. **Phantom `GET /api/reqs` (list)** — UI hits it, returns 404 because no such endpoint exists. Pipeline list view is broken for that reason.
2. **Comp estimation conservatism** — uses posted JD range instead of market estimates. Prompt tuning needed.
3. **AIR Blackbox 4 deferred FAILs** — trust layer install, kill switch, vault config, guardrails.yaml.
4. **Resend domain verification** for sourcingnav.com (still sandbox-only delivery to info@nostalgicskinco.com).
5. **AUDIT_HMAC_KEY** separate from MAGIC_LINK_SECRET (production currently uses fallback).
6. **Resolver problem at intake** — AI emits raw_skill_text instead of recognizing existing canonicals. Not a taxonomy problem. Two possible fixes for later: pass skills list into JD parser prompt OR add post-processing fuzzy match.

## Phase B status going into next session

- B1: ✅ shipped, validated end-to-end with Steve Benz transition
- B2: ✅ shipped, validated with 18 decisions, 6%→33% resolution rate
- B3: 🚧 Next — role archetype discovery via JD clustering
- B4: ⏸ Awaiting more data — skill mesh co-occurrence

## Next session: Phase B3 plan

Cluster JDs to discover emerging role archetypes (Talent Engineer, Prompt Engineer, Embedded ML Engineer, Data Engineer→Full-Stack hybrid, etc.).

### Key design questions to resolve before building

1. **Clustering algorithm.** Options: k-means on skill vectors, hierarchical clustering on co-occurrence, LLM-as-judge clustering. My lean: hybrid — embed JDs with skill vectors, k-means for grouping, LLM to NAME the resulting clusters.
2. **What we actually surface.** A "discovered archetype" needs a name, a skill profile, example reqs. Where does it live in the schema?
3. **How does it feed back into matching.** Does an archetype affect adjacency weights? Does it change how candidates get scored?

### Pre-flight before B3

We need MORE THAN 7 reqs to cluster meaningfully. Either:
- Run intake on 5-10 more diverse JDs first
- Defer B3 until natural usage produces more reqs
- Accept that early clustering will be noisy and iterate

### Recommendation

Don't build B3 next. Build the **resolver fix** (post-processing fuzzy match for raw_skill_text) instead. It will boost resolution rate to ~60-70% on existing data without needing manual taxonomy work, and it's a 1-2 hour build. THEN do B4 (skill mesh) which uses that cleaner data. THEN B3.

Better next-session order: resolver fix → B4 → B3.
